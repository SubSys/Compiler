module Main exposing (..)

import Native.Core


(+) : a -> a -> a
(+) left right =
    Native.Core.plus left right

alpha : Int
alpha =
    (1 + 2) + 3

beta : Int -> Int -> Int -> Int
beta x y z =
    x + y + z

gamma : Int -> Int
gamma z =
    beta 1 2 z

